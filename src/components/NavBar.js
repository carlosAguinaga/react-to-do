import React from "react";

const NavBar = () => {
    
  return (
    <div className="container">
      <nav className="navbar bg-primary">
        <button>login</button>
      </nav>
    </div>
  );
};

export default NavBar;
